#ModPacker

ModPacker is a modular JavaScript/Node.js library specifically designed to handle bit packing and bitmasking in industrial communication protocols like Modbus.

This library allows you to efficiently pack up to 16, 32, 64, or even 128 sensor/actuator states (boolean) into one or more decimal data registers, significantly reducing network query overhead.